import React from 'react';
import './Checkout.css'

const Checkout = () => {
    return (
        <div className='checkout'>
            <button>checkout</button>
        </div>
    );
};

export default Checkout;